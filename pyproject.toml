[project]
name = "traveltom"
version = "0.1.0"
description = "TravelTom backend services and tooling"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
  "azure-identity>=1.17.1",
  "azure-monitor-opentelemetry>=1.6.0",
  "azure-storage-blob>=12.24.0",
  "fastapi>=0.110",
  "fastapi-users[sqlalchemy]>=15.0.4",
  "alembic>=1.13",
  "fastapi-azure-auth>=5.2.0",
  "langchain>=1.0",
  "langchain-core>=1.2",
  "langchain-ollama>=1.0",
  "langchain-openai>=1.0",
  "limits>=5.8",
  "numpy>=1.26",
  "pandas>=2.2",
  "pyarrow>=15.0",
  "pydantic>=2.6",
  "pydantic-settings>=2.2",
  "pgvector>=0.2.5",
  "opentelemetry-api>=1.33.0",
  "opentelemetry-instrumentation-fastapi>=0.54b0",
  "opentelemetry-instrumentation-logging>=0.54b0",
  "opentelemetry-instrumentation-sqlalchemy>=0.54b0",
  "opentelemetry-sdk>=1.33.0",
  "sqlalchemy>=2.0",
  "asyncpg>=0.29",
  "uvicorn[standard]>=0.27",
]

[project.optional-dependencies]
dev = [
  "black==26.3.1",
  "httpx>=0.28",
  "mypy==1.19.1",
  "pytest==9.0.2",
  "ruff==0.15.9",
]
ranking-ml = [
  "lightgbm>=4.3",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["traveltom*"]
exclude = ["apps*", "docs*", "examples*", "infra*", "instructions*", "scripts*", "tests*", "venv*"]

[tool.black]
line-length = 88
target-version = ["py310"]

[tool.ruff]
line-length = 88
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.mypy]
python_version = "3.10"
warn_unused_configs = true
ignore_missing_imports = true
plugins = ["pydantic.mypy"]
exclude = '''(?x)(
    ^build[\\/]
  | ^dist[\\/]
  | ^venv[\\/]
  | ^\.venv[\\/]
  | ^traveltom\.egg-info[\\/]
)'''
